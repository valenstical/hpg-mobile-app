import React from "react";
import { SafeAreaView, View, Text, ScrollView, StatusBar } from "react-native";

import { styles, bootstrap } from "./styles";

const Worksheet = ({ navigation }) => {
  return (
    <SafeAreaView>
      <Text>Worsheet </Text>
    </SafeAreaView>
  );
};
export default Worksheet;
